---
name: "otp_rebar"
runtimes:
  - erlangR14B02:
      version: <%= "#{node[:erlang][:version]}" %>
      description: <%= "Erlang #{node[:erlang][:version]}" %>
      executable: <%= File.join(node[:erlang][:path], "bin", "erl") %>
      default: true
app_servers:
detection:
  - "releases/*/*.rel": '.'
staged_services:
